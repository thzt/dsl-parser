{
  "name": "dsl-parser",
  "version": "1.0.0",
  "description": "",
  "main": "out/index.js",
  "scripts": {
    "watch": "trash out && tsc -w -p .",
    "test": "mocha --require ts-node/register test/**/*.ts -timeout 100000",
    "debug:test": "node --inspect=5858 node_modules/.bin/mocha --require ts-node/register test/**/*.ts -timeout 100000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/mocha": "^10.0.7",
    "@types/node": "^20.14.9",
    "mocha": "^10.5.2",
    "trash-cli": "^5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2"
  }
}
